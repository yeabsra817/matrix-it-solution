import { makeRoleLayout } from "@/lib/role-pages";

export default makeRoleLayout("IT_SUPPORT", [
  { href: "/dashboard/it-support", label: "IT Support" },
  { href: "/dashboard/it-support/settings", label: "Settings" },
]);
